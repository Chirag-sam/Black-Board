<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.bb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".stafftimetable" />
        <activity android:name=".displaytable" />
        <activity android:name=".availablestaff" />
        <activity android:name=".displaystaff" />
        <activity android:name=".findclass" />
        <activity android:name=".classtimetable" />
        <activity android:name=".displayclasstable"></activity>
    </application>

</manifest>